foreach(t decoder_test compare_test detect_test)
    add_executable(${t} ${t}.c)
    target_link_libraries(${t} PRIVATE drift_viterbi::drift_viterbi)
    add_test(NAME ${t} COMMAND ${t})
endforeach()
